import { Form, MenuOptions } from "../../components/auth-components";

export const Login = () => {
    return (
        <div className="h-full pl-28 flex items-start">
            <MenuOptions />
            <Form />
        </div>
    );
};
